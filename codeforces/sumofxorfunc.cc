#include <iostream>
#include <utility>
using namespace std;
using ll = long long;
typedef pair<ll, ll> node;
int n;
node dp[300000][300000];
long long ar[100000];
int main() {
  cin >> n;
  /* for (int i = 0; i < n; i++) { */
  /*   cin >> ar[i]; */
  /*   dp[i][i] = {ar[i], ar[i]}; */
  /* } */
  /* for (int i = 1; i < n; i++) { */
  /*   for (int j = 1; j < n; j++) */
  /*     if (i + j < n) { */
  /*     } */
  /* } */
  /* cout << dp[n-1][n-1].first << endl; */
  return 0;
}
