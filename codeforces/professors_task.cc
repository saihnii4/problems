// https://codeforces.com/problemset/problem/70/D
#include <iostream>
using namespace std;
int q, op, x, y;
int main() {
  cin >> q;
  for (int i=0;i<q;i++) {
    cin >> op >> x >> y;
    if (op == 1) {

    } else {

    }
  }
}
