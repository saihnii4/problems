#include <iostream>
using namespace std;
using ll = long long;
int main() {
  int n, q;
  cin >> n >> q;
  int op, a;
  ll b, c;
  for (int i=0;i<q;i++) {
    cin >> op >> a >> b;
    if (op == 1) {
      cin >> c;
    } else {
    }
  }
}
