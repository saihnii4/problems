#include <iostream>
#include <string>
using namespace std;
int t;
int main() {
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    string str;
    getline(cin, str);
    for (int i=0;i<n;i++) {
      str.c_str()[i]
    }
  }
}
