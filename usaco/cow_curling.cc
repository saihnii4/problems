#include <iostream>
using namespace std;
using ll = long long;
int n;
int main() {
  freopen("curling.in","r",stdin);
  freopen("curling.out","w",stdout);
  cin >> n;
  for (int i=0;i<n;i++) {
    ll x, y;
    cin >> x >> y;
  }
  for (int i=0;i<n;i++) {
    ll x, y;
    cin >> x >> y;
  }
}
