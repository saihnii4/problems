#include <iostream>
using namespace std;
using ll = long long;
int n;
int main() {
  cin.tie(NULL);
  ios_base::sync_with_stdio(false);
  freopen("hps.in", "in", stdin);
  freopen("hps.out", "out", stdout);
  cin >> n;
  for (int i=0;i<n;i++) {
    char t;
    cin >> t;

  }
}
