#include <stdio.h>

int a, b, c, d, n;

int flowers = 0;

int min(int a, int b) { return (a > b) ? a : b; }

int maybe(int n, int flowers) {
    if (n == )
    return min(maybe(n-1, flowers-a), maybe(n-1, flowers-c));
}

int main() {
    int a, b, c, d, n;
    scanf("%d %d %d %d %d", &n, &a, &b, &c, &d);


}
