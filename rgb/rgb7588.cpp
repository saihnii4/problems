// TODO:

#include <stdio.h>
#include <vector>
#include <iostream>

int main() {
    int n;
    scanf("%d", &n);

    while (n--) {
        int a;
        std::vector<int> b;
        std::cin >> a;
        b.push_back(a);
    }
}
