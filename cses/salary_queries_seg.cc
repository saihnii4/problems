#include <iostream>
#include <vector>
using namespace std;
using ll = long long;
int n,q;
int main() {
  cin >> n >> q;
  vector<ll> arr(200000);
  for (int i=0;i<n;i++) {
    cin >> arr[i];
  }
  for (int i=0;i<q;i++) {
    int op;
    ll a, b;
    cin >> op >> a >> b;
    if (op - '?' == 0) {

    } else {

    }
  }
}
