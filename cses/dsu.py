from __future__ import annotations
from dataclasses import dataclass

@dataclass
class Node:
    val: int
    representative: int
    parent: Node

    def merge(self, i, j):
        pass

    def find(self, i):
        if (i)

class Tree:
    pass
