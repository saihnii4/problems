#include <stdio.h>

typedef long long ll;

int n;
ll dp[1000][1000];

int main() { 
    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            scanf("%d", );
        }
    }
}