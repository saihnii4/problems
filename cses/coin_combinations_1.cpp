#include <stdio.h>

using namespace std;

int n;
long long k, dp[1000001];

int main() {
  scanf("%d %lld", &n, &k);
  long long coins[n];

  for (int i = 0; i < n; i++) {
    scanf("%lld", &coins[i]);
  }

  for (int i = 1; i <= n; i++) {

  }
}
