#include <stdio.h>
#include <string>

int main() {
    int T;
    scanf("%d", &T);

    for (int i = 0; i < N; i++) {
        int N;
        scanf("%d", &N);
        
        string line;
        cin.ignore();
        getline(cin, line);

        istringstream ss(line);

    }
    return 0;
}
