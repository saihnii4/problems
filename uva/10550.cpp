#include <stdio.h>

int main() {
    int degrees = 720;
    while (;;) {
        int pos, a, b, c;
        scanf("%d %d %d %d", &pos, &a, &b, &c);
        if (pos == 0 && a == 0 && b == 0 && c == 0) break;

        
    }
    return 0;
}
