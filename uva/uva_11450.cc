// TODO:
#include <iostream>
using namespace std;
using ll = long long;
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  freopen("sample", "r", stdin);
  int t;
  cin >> t;
  while (t--) {
    int m, c;
    cin >> m >> c;
    for (int i=0;i<m;i++) {
      int models;
      cin >> models;
      for (int j=0;j<models;j++) {
        cin >> t;
      }
    }
  }
}
